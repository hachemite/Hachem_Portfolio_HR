name: '🚀 Production Deployment'

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main]
  workflow_dispatch: 

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      deployments: write
      pull-requests: write

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: 📦 Install & QA
        # We combine these so if QA fails, the build never starts (SecOps best practice)
        run: |
          npm ci
          npm run qa 

      - name: 🏗️ Build
        run: npm run build
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: ☁️ Deploy to Cloudflare
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: ${{CLOUDFLARE_PROJECT_NAME}}
          directory: 'dist'
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
